export class Receipt
{
  
    constructor(public sum?: number, public totalSum?: number, public count?: number,
        public sumDiscount?: number, public birthday: boolean = false
    ) {
        
    }
   
}